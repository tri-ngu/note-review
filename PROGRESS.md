# Progress & Todos — Note Review (v1)

## Done

### Planning & requirements docs

- Established domain terminology in `CONTEXT.md` (Note, Question Set, Question, Multiple-Choice/Select-All, Review Session, Summary, Concept, Analyzer, Generator, Verifier, v2 Room/Host/Player).
- Wrote `requirements.md` v1 functional scope (input constraints, generation rules, Review Session behavior, error handling, platform/persistence) plus v1.x/v2 planned-version notes.
- Evaluated `SessionStore` backing-storage options; recommended lock-guarded nested hashmap as the structure that carries unchanged from v1 into v2 Room state.
- Wrote `DESIGN.md`: generation pipeline (Analyzer → user checkpoint → Generator → Verifier loop → freeze → flashcard display → in-place editing), 3-agent architecture (Analyzer/Generator/Verifier, Python-orchestrated loop capped at 5 verify iterations), and full data model (`ConceptAllocation`, `Question`, `QuestionSet`, Pydantic-validated fixed sizes).
- Created GitHub repo (`note-review`) to store progress, plus a separate `flashcard-prototype` branch for the first barebone flashcard implementation.
- Grilling session: resolved a stale NVIDIA NIM → Groq (`deepseek-r1-distill-llama-70b`) inconsistency between `requirements.md` and `DESIGN.md`/`CONTEXT.md` (flagging the model-specific empirical claims in `DESIGN.md` — speed, rate limit, quirks — as unverified until re-tested against the new model); added Intended User & Needs, consolidated Non-Goals, and a Given/When/Then Acceptance Criteria section to `requirements.md`; fixed v1.x ordering and v2-after-v1.x sequencing.
- `DESIGN.md` rework: added Capacity map (upload/rate-limit/verify-loop/Room caps), API contract (FastAPI endpoints), Extraction section (`[Page N]` heading format, matches `water_cycle_note.txt`), Concurrency cap plan for the Generator/Verifier fan-out; reworked Pipeline for per-concept Question lists with a global index assigned upfront (was: single flat list from the start), and added deterministic largest-remainder reconciliation for per-concept `question_count` (mirrors the existing weight-drift correction). Live dummy call to Groq confirmed `deepseek-r1-distill-llama-70b` is decommissioned (HTTP 400, `model_decommissioned`) — model swapped to `gpt-oss-120b` throughout `DESIGN.md`, with its free-tier rate limits (30 RPM, 1K RPD, 8K TPM, 200K TPD) recorded in Capacity map.
- Drafted and iterated actual agent prompts (Analyzer, Generator initial/patch, Verifier) in `agents.txt`, switching from an SDK-level structured `output_type` to free-text `--START--/--END--`-delimited output parsed deterministically by Python; settled that Python (not the Analyzer) derives/decides `question_count` and the default total when unspecified. Merged the finished `## Agents` + `## Agent prompts` content from `agents.txt` into `DESIGN.md`.
- Consistency pass across `CONTEXT.md`/`requirements.md`/`DESIGN.md`: updated `CONTEXT.md`'s Required Documents (Capacity map, API contract now marked existing), provider/model name (`gpt-oss-120b`), and Analyzer/Generator definitions to match the Python-derives-question_count architecture; updated `requirements.md`'s generation-rules wording to match.
- All 5 card themes in `assets/card-themes/` (Botanical Field Guide, Chalkboard Classroom, Neubrutalist Quiz Show, Riso Print Zine, Vintage Postcard) standardized to one card size (`min(420px, 92vw)` × 540px) with no scrolling needed, and stripped down to just flashcard view + Prev/Next + Card X/total — ready to use as-is. Prototype build will only wire up the default (Botanical Field Guide); the other four stay saved for the later theme-switching build.
- Grilled and designed the v2 Game Room mechanic end-to-end (2026-08-10), in isolation on the `game-room-design` orphan branch/worktree (only `CONTEXT.md`/`DESIGN.md`/`PROGRESS.md`/`requirements.md` copied over) before merging: Room pipeline (creation → LOBBY → QUESTION_ACTIVE → ANSWER_REVEAL → LEADERBOARD → FINISHED → cleanup), state machine, join flow, one-shot answer submission (tap-to-submit Multiple-Choice, multi-tap-then-Submit Select-All) with server-authoritative timing, all-or-nothing scoring plus standard/skip leaderboard ranking, `PlayerState`/`RoomState`/`Answer` data model, 5-minute TTL cleanup, full WebSocket message protocol, and new `POST /rooms` / `POST /rooms/{pin}/join` / `GET /rooms/{pin}` / `WS /ws/room/{pin}` endpoints. Scoped to skip the real v1 hub/pipeline for now — one hardcoded prewritten `QuestionSet` fixture, no agent-generated questions — explicitly flagged as deferred in `DESIGN.md`'s Room mode — deferred / out of scope section. Merged wholesale into the main `DESIGN.md` (new Room sections, plus folded into the existing Capacity map/API contract/Data model sections), with a cross-check pass against `CONTEXT.md`/`requirements.md` that caught and fixed: a missing Question-shuffle step (`requirements.md`'s "cycle in random order"), a Lobby/Game terminology clarification added to `CONTEXT.md`'s Room avoid-list, a `GET /rooms/{pin}` status-enum documentation gap, and 3 dangling `storage-options.md` citations (the file never existed in git history — dropped, not recreated). The design draft (`ROOM-DESIGN.md`) and pre-merge doc snapshots are kept as a historical record on the `game-room-design` branch, not pushed to `main`.

### Game Room (`game-room-build` branch, separate local clone at `game-room-build/`, own `.git`)

Built in a separate local clone per Tri's request, so implementation edits never touch `CONTEXT.md`/`DESIGN.md`/`PROGRESS.md`/`requirements.md` directly. One hardcoded fixture (`WATER_CYCLE_QUESTION_SET`, 8 hand-written questions), not the real v1 upload/Analyzer/Generator/Verifier pipeline — see `CONTEXT.md`'s note on this.

- **Backend** (`backend/`, FastAPI + Pydantic) — data model + store (`app/models.py`, `app/store.py`: PIN-keyed in-memory `RoomStore`, TTL sweep), HTTP endpoints (`app/rooms_http.py`: `POST /rooms`, `POST /rooms/{pin}/join`, `GET /rooms/{pin}`), WS protocol + state machine (`app/rooms_ws.py`: LOBBY→QUESTION_ACTIVE→ANSWER_REVEAL→LEADERBOARD→FINISHED, server-authoritative 30s timer, Host-disconnect short-circuit), game logic (`app/game_logic.py`: scoring brackets, all-or-nothing Select-All correctness, standard/skip ranking). Built test-first, 76 pytest cases (happy path plus adversarial: malformed WS messages, one-shot answer lock, mid-game disconnect, full 8-round game, 3-player tie ranking, `RoomStore` concurrency). Two real bugs found and fixed: a `store.mutate()` re-entry deadlock in the leaderboard→finish path, and a missing model-level check that Multiple-Choice questions have exactly 1 correct answer. Manually smoke-tested live over `uvicorn`.
- **Frontend** (`frontend/`, extends the existing Vite + React + TS app) — designed via `docs/superpowers/specs/2026-08-11-game-room-frontend-design.md` (brainstormed, merged into `DESIGN.md`'s Room sections) and `docs/superpowers/plans/2026-08-11-game-room-frontend-implementation.md`, both on the `game-room-build` clone (2026-08-11). `react-router` (4 routes: `/` gains a "Create Room" button, `/host/:pin`, `/join/:pin`, `/play/:pin`) and `qrcode.react` added as new dependencies; Vite dev proxy to the backend's `localhost:8000`. One `useRoomSocket` hook per connection wraps a pure `roomSocketReducer` (unit-tested, 10 cases) driving 8 phase-specific panels (`LobbyPanel`, `QuestionActivePanel`, `LeaderboardPanel`, `FinishedPanel`, `WaitingPanel`, `QuestionPanel`, `RevealPanel`, `RoomErrorPanel`) — Host and Player pages each just switch panels by `RoomState.status`. `/join/:pin` calls `GET /rooms/{pin}` as a pre-check before showing the nickname form (per `DESIGN.md`'s API contract). No WS auto-reconnect; page-reload/resume mid-game not built (only an already-ended Room is detected gracefully, via the same precheck, now on both Host and Player). 49/49 tests pass (12 new), `tsc --noEmit`/`oxlint` clean. Manually smoke-tested live (two browser tabs, Host + Player) via Claude driving Chrome directly: room creation, live roster join, Multiple-Choice and Select-All rounds, the 30s server-timeout path, leaderboard, Host end-game, Finished screen on both roles, "Back to Home" routing — no console errors.
- Both pushed to `origin/game-room-build` (backend earlier; frontend 2026-08-11, 17 commits).
- **Manual test pass** (2026-08-11) found 7 flags; Flag #2 (no Join Room entry point) and flags #1/#3-#7 (reveal-phase gating, duplicate-connection handling, host countdown, host reload precheck, leaderboard button) all fixed — see `GAME-ROOM-MANUAL-TEST-FLAGS.md` on the `game-room-build` branch for detail per-flag. Frontend `tsc`/`vitest` re-verified clean after fixes; backend `pytest` re-run still pending as of this merge.
- **Merged into `main`** (2026-08-11) ahead of the original "wait for all v1.x to ship" plan, per Tri's explicit call — see `CONTEXT.md`'s highlighted note: Room is wired to the hardcoded fixture, not the real agent pipeline, until v1 generation is finished and reconnected.

### Flashcard prototype (`frontend/`, Vite + React + TS)

Built off a hand-written (not agent-generated) fixture Question Set to validate the display/UX independent of the generation pipeline, styled off the Botanical Field Guide reference in `assets/card-themes/`. Split into three builds:

- Build 1 — flashcard display and navigation only (front/back flip, Next/Prev, progress indicator), no editing yet. Grilled and scoped (2026-08-10): `frontend/` dir, Vite + React + TS + npm; CSS Modules porting `botanical-field-guide.html` near-verbatim; TS types mirroring `DESIGN.md`'s data model; fixture as a `.ts` file (`satisfies QuestionSet`, Claude drafted content grounded in `water_cycle_note.txt`, Tri reviewed before it was saved); components `FlashcardView` + `Flashcard` + a plain concept-trim helper function; Vitest unit test for the concept-trim helper only; disabled placeholder Edit button on the card back. Built and verified (2026-08-10): Claude drove Chrome directly against the dev server — flip, Next/Prev with correct boundary disabling, flip-reset-on-nav (via `key={currentIndex}` remount), radio vs checkbox mark shapes, correct-answer highlighting, concept-trim rule (both delimiter and no-delimiter cases), disabled Edit placeholder all confirmed live; no console errors; `npm test` (6/6) and `tsc --noEmit` clean. `water_cycle_note.txt` also removed from `main` (kept on this branch only) at Tri's request, since it only grounds this branch's fixture.
- Build 2 — option to start a Review Session (per `CONTEXT.md`/`requirements.md`'s definition), reachable via a "Start Review" button on the flashcard display. Built and verified (2026-08-10).
- Build 3 — in-place editing from the flashcard view (per DESIGN.md's Editing section: field edits, Save/Cancel, concept rename/reassign). Built and verified (2026-08-10).



## Current State

`requirements.md` considered done — scope fully defined (intended user, needs, use cases, non-goals, acceptance criteria, planned-version ordering). `DESIGN.md` rework considered done: Capacity map, API contract, and Extraction sections added; Pipeline/Concurrent generation/Snippet grounding/Weight reconciliation updated for the per-concept-list restructure and the `gpt-oss-120b` model swap; actual Agent prompts (Analyzer/Generator/Verifier) written, iterated, and merged in from `agents.txt`. `CONTEXT.md` and `requirements.md` cross-checked against `DESIGN.md` and brought back in sync. All three prototype builds are done: Build 1 (display/nav), Build 2 (Review Session), Build 3 (in-place editing, including concept rename/reassign). Agents implemented, tested, and pipeline-wired on the `agent-testing` branch (see Todo) — a split-vs-big-call design comparison is in progress there, currently blocked on Groq's daily token cap. Game Room (v2) backend and frontend are both built, tested, and pushed to `origin/game-room-build` (2026-08-11) — see Done above; connecting it to the real v1 hub/pipeline stays deferred per `DESIGN.md`.

## Todo

- [x] Game Room implementation, backend + frontend (parts 1-8) — see Done above. Built and pushed to `origin/game-room-build` (2026-08-11).
- [x] Game Room manual-test flags (all 7) fixed and merged into `main` (2026-08-11) — see Done above. Ahead of the original "after all v1.x ships" plan, per Tri's explicit call to bring it in now with the fixture caveat flagged in `CONTEXT.md`.
- [ ] Re-run backend `pytest` on the merged/cleaned-up `main` (deferred until after this merge + cleanup pass, per Tri).
- [ ] Once v1 generation (Analyzer/Generator/Verifier pipeline) is finished: reconnect Room creation to the real generated `QuestionSet` instead of the hardcoded fixture — see `CONTEXT.md`'s note and `DESIGN.md`'s Room mode section.

- [x] Agents implemented and pipeline-wired (`agent-testing` branch) — isolated per-call tests done (`agent_testing/agent-test-log.md`), full pipeline (Analyzer → checkpoint → Generator → verify loop → Freeze) wired and run once clean end-to-end (`agent_testing/pipeline-run-log.md`). Two real prompt bugs found and fixed via live testing: Generator-patch was ignoring new fix snippets and reusing the pre-fix `source_quote`; Verifier was flagging `is_select_all` purely from `len(correct_answers)` instead of phrasing/framing. Both fixes copied into `DESIGN.md`'s canonical prompts after confirming with Tri.
- [x] Split-vs-big-call comparison run (2026-08-11), same input (`water_cycle_note.txt`) for all 3 tests: `agent-testing` sequential succeeded clean (27 Questions, 290.5s, 0 rate-limit hits, 163/163 quality checks). `agent-testing` concurrent succeeded but gained nothing (24 Questions, 298.1s — no faster than sequential — uncapped `asyncio.gather` triggered 21 rate-limit hits that serialized it back into a queue anyway; concurrency needs a tuned semaphore cap to actually help, not uncapped `gather`). `agent-testing-bigcall` failed both attempts — the single Generator call asking for all ~24 questions at once got truncated before completing (not a 429; likely the model's default completion-token cap, worse for `gpt-oss-120b` since it's a reasoning model that burns completion budget on hidden reasoning before any visible output). **Decision: drop the big-call approach** — `agent-testing-bigcall` branch deleted (local + remote), its worktree removed. Split design only, going forward. Full comparison + per-call logs kept in a local `agent-comparison-report.md` at the repo root (Tri's copy only, not committed).
  - Next: token/runtime optimization on the split design, since each full run costs ~1/4 of Groq's daily token budget (only ~4 such runs/day possible). Proposed, ranked by impact: (1) capped concurrency (semaphore, tune N against the 8K TPM ceiling) — already flagged as a TODO in DESIGN.md's Concurrency cap section, test 2 above confirms uncapped doesn't work; (2) full-Note-to-Verifier is the single biggest token cost (~65% of input tokens, sent on every per-concept Verifier call) but is a deliberate DESIGN.md safety-net decision (Snippet grounding section) — not touched, flagged for Tri's call rather than acted on unilaterally; (3) [done] trimmed the Analyzer/Generator worked examples in DESIGN.md's Agent prompts (Verifier wording explicitly left untouched, per Tri) — open as draft PR #1, not yet merged, savings not yet re-measured against a full pipeline run; (4) batch 2-3 concepts per Generator/Verifier call as a middle ground between split (9 calls/stage) and big-call (1 call, broke) — not started; (5) explicit `max_tokens` cap on every call, so a truncation failure fails fast on attempt 1 instead of wasting 3 full-price retries like big-call's did — not started.



## Deferred (not started, tracked in `requirements.md` Planned Versions)

- Theme switching — wiring the four saved reference themes (`assets/card-themes/`) in as selectable alternatives to the default.
- v1.x: multi-PDF merge, regeneration controls, difficulty levels, larger file support, spaced-repetition/requeue mechanics.
- v2: connecting the built Room mode to the real v1 hub/generated `QuestionSet` (currently one hardcoded fixture) — see `DESIGN.md`'s Room mode — deferred / out of scope.

