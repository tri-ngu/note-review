# Progress & Todos — Note Review (v1)

## Done

### Planning & requirements docs

- Established domain terminology in `CONTEXT.md` (Note, Question Set, Question, Multiple-Choice/Select-All, Review Session, Summary, Concept, Analyzer, Generator, Verifier, v2 Room/Host/Player).
- Wrote `requirements.md` v1 functional scope (input constraints, generation rules, Review Session behavior, error handling, platform/persistence) plus v1.x/v2 planned-version notes.
- Evaluated `SessionStore` backing-storage options; recommended lock-guarded nested hashmap as the structure that carries unchanged from v1 into v2 Room state.
- Wrote `DESIGN.md`: generation pipeline (Analyzer → user checkpoint → Generator → Verifier loop → freeze → flashcard display → in-place editing), 3-agent architecture (Analyzer/Generator/Verifier, Python-orchestrated loop capped at 5 verify iterations), and full data model (`ConceptAllocation`, `Question`, `QuestionSet`, Pydantic-validated fixed sizes).
- Created GitHub repo (`note-review`) to store progress, plus a separate `flashcard-prototype` branch for the first barebone flashcard implementation.
- Grilling session: resolved a stale NVIDIA NIM → Groq (`deepseek-r1-distill-llama-70b`) inconsistency between `requirements.md` and `DESIGN.md`/`CONTEXT.md` (flagging the model-specific empirical claims in `DESIGN.md` — speed, rate limit, quirks — as unverified until re-tested against the new model); added Intended User & Needs, consolidated Non-Goals, and a Given/When/Then Acceptance Criteria section to `requirements.md`; fixed v1.x ordering and v2-after-v1.x sequencing.
- `DESIGN.md` rework: added Capacity map (upload/rate-limit/verify-loop/Room caps), API contract (FastAPI endpoints), Extraction section (`[Page N]` heading format, matches `water_cycle_note.txt`), Concurrency cap plan for the Generator/Verifier fan-out; reworked Pipeline for per-concept Question lists with a global index assigned upfront (was: single flat list from the start), and added deterministic largest-remainder reconciliation for per-concept `question_count` (mirrors the existing weight-drift correction). Live dummy call to Groq confirmed `deepseek-r1-distill-llama-70b` is decommissioned (HTTP 400, `model_decommissioned`) — model swapped to `gpt-oss-120b` throughout `DESIGN.md`, with its free-tier rate limits (30 RPM, 1K RPD, 8K TPM, 200K TPD) recorded in Capacity map.
- Drafted and iterated actual agent prompts (Analyzer, Generator initial/patch, Verifier) in `agents.txt`, switching from an SDK-level structured `output_type` to free-text `--START--/--END--`-delimited output parsed deterministically by Python; settled that Python (not the Analyzer) derives/decides `question_count` and the default total when unspecified. Merged the finished `## Agents` + `## Agent prompts` content from `agents.txt` into `DESIGN.md`.
- Consistency pass across `CONTEXT.md`/`requirements.md`/`DESIGN.md`: updated `CONTEXT.md`'s Required Documents (Capacity map, API contract now marked existing), provider/model name (`gpt-oss-120b`), and Analyzer/Generator definitions to match the Python-derives-question_count architecture; updated `requirements.md`'s generation-rules wording to match.
- All 5 card themes in `assets/card-themes/` (Botanical Field Guide, Chalkboard Classroom, Neubrutalist Quiz Show, Riso Print Zine, Vintage Postcard) standardized to one card size (`min(420px, 92vw)` × 540px) with no scrolling needed, and stripped down to just flashcard view + Prev/Next + Card X/total — ready to use as-is. Prototype build will only wire up the default (Botanical Field Guide); the other four stay saved for the later theme-switching build.
- Grilled and designed the v2 Game Room mechanic end-to-end (2026-08-10), in isolation on the `game-room-design` orphan branch/worktree (only `CONTEXT.md`/`DESIGN.md`/`PROGRESS.md`/`requirements.md` copied over) before merging: Room pipeline (creation → LOBBY → QUESTION_ACTIVE → ANSWER_REVEAL → LEADERBOARD → FINISHED → cleanup), state machine, join flow, one-shot answer submission (tap-to-submit Multiple-Choice, multi-tap-then-Submit Select-All) with server-authoritative timing, all-or-nothing scoring plus standard/skip leaderboard ranking, `PlayerState`/`RoomState`/`Answer` data model, 5-minute TTL cleanup, full WebSocket message protocol, and new `POST /rooms` / `POST /rooms/{pin}/join` / `GET /rooms/{pin}` / `WS /ws/room/{pin}` endpoints. Scoped to skip the real v1 hub/pipeline at the time — one hardcoded prewritten `QuestionSet` fixture, no agent-generated questions (see `CONTEXT.md`'s note on this — Room later shipped to `main` still wired this way, ahead of the original plan). Merged wholesale into the main `DESIGN.md` (new Room sections, plus folded into the existing Capacity map/API contract/Data model sections), with a cross-check pass against `CONTEXT.md`/`requirements.md` that caught and fixed: a missing Question-shuffle step (`requirements.md`'s "cycle in random order"), a Lobby/Game terminology clarification added to `CONTEXT.md`'s Room avoid-list, a `GET /rooms/{pin}` status-enum documentation gap, and 3 dangling `storage-options.md` citations (the file never existed in git history — dropped, not recreated). The design draft (`ROOM-DESIGN.md`) and pre-merge doc snapshots lived as a historical record on the `game-room-design` branch/worktree until it was deleted (2026-08-11) once Room mode fully shipped to `main` and the branch had nothing left to contribute.

### Game Room (`game-room-build` branch, separate local clone at `game-room-build/`, own `.git`)

Built in a separate local clone per Tri's request, so implementation edits never touch `CONTEXT.md`/`DESIGN.md`/`PROGRESS.md`/`requirements.md` directly. One hardcoded fixture (`DIGESTIVE_SYSTEM_QUESTION_SET`, 8 hand-written questions — originally water-cycle-themed, replaced 2026-08-11 during full testing to get fresh test content), not the real v1 upload/Analyzer/Generator/Verifier pipeline — see `CONTEXT.md`'s note on this.

- **Backend** (`backend/`, FastAPI + Pydantic) — data model + store (`app/models.py`, `app/store.py`: PIN-keyed in-memory `RoomStore`, TTL sweep), HTTP endpoints (`app/rooms_http.py`: `POST /rooms`, `POST /rooms/{pin}/join`, `GET /rooms/{pin}`), WS protocol + state machine (`app/rooms_ws.py`: LOBBY→QUESTION_ACTIVE→ANSWER_REVEAL→LEADERBOARD→FINISHED, server-authoritative 30s timer, Host-disconnect short-circuit), game logic (`app/game_logic.py`: scoring brackets, all-or-nothing Select-All correctness, standard/skip ranking). Built test-first, 76 pytest cases (happy path plus adversarial: malformed WS messages, one-shot answer lock, mid-game disconnect, full 8-round game, 3-player tie ranking, `RoomStore` concurrency). Two real bugs found and fixed: a `store.mutate()` re-entry deadlock in the leaderboard→finish path, and a missing model-level check that Multiple-Choice questions have exactly 1 correct answer. Manually smoke-tested live over `uvicorn`.
- **Frontend** (`frontend/`, extends the existing Vite + React + TS app) — designed via `docs/superpowers/specs/2026-08-11-game-room-frontend-design.md` (brainstormed, merged into `DESIGN.md`'s Room sections) and `docs/superpowers/plans/2026-08-11-game-room-frontend-implementation.md`, both on the `game-room-build` clone (2026-08-11). `react-router` (4 routes: `/` gains a "Create Room" button, `/host/:pin`, `/join/:pin`, `/play/:pin`) and `qrcode.react` added as new dependencies; Vite dev proxy to the backend's `localhost:8000`. One `useRoomSocket` hook per connection wraps a pure `roomSocketReducer` (unit-tested, 10 cases) driving 8 phase-specific panels (`LobbyPanel`, `QuestionActivePanel`, `LeaderboardPanel`, `FinishedPanel`, `WaitingPanel`, `QuestionPanel`, `RevealPanel`, `RoomErrorPanel`) — Host and Player pages each just switch panels by `RoomState.status`. `/join/:pin` calls `GET /rooms/{pin}` as a pre-check before showing the nickname form (per `DESIGN.md`'s API contract). No WS auto-reconnect; page-reload/resume mid-game not built (only an already-ended Room is detected gracefully, via the same precheck, now on both Host and Player). 49/49 tests pass (12 new), `tsc --noEmit`/`oxlint` clean. Manually smoke-tested live (two browser tabs, Host + Player) via Claude driving Chrome directly: room creation, live roster join, Multiple-Choice and Select-All rounds, the 30s server-timeout path, leaderboard, Host end-game, Finished screen on both roles, "Back to Home" routing — no console errors.
- Both pushed to `origin/game-room-build` (backend earlier; frontend 2026-08-11, 17 commits).
- **Manual test pass** (2026-08-11) found 7 flags; Flag #2 (no Join Room entry point) and flags #1/#3-#7 (reveal-phase gating, duplicate-connection handling, host countdown, host reload precheck, leaderboard button) all fixed and verified — see full test pass below.
- **Merged into `main`** (2026-08-11) ahead of the original "wait for all v1.x to ship" plan, per Tri's explicit call — see `CONTEXT.md`'s highlighted note: Room is wired to the hardcoded fixture, not the real agent pipeline, until v1 generation is finished and reconnected.
- **Full test pass** (2026-08-11/12), everything except agent question generation — flashcard view, Review Session, and the entire Game Room, covering edge cases (all Select-All shapes including all-4-correct and single-correct-checkbox, tie/skip ranking, duplicate nickname/connection, room-not-found/already-started/room-closed precheck errors, Host disconnect, End Game early, question timeout with no answers). Question set fixture replaced water-cycle → digestive system (`DIGESTIVE_SYSTEM_QUESTION_SET`, same shape/edge-case coverage) for fresh test content, in both `backend/app/fixture.py` and `frontend/src/fixtures/fixture.ts`. Two real bugs found and fixed during this pass, beyond the 7 manual-test flags:
  - **StrictMode double-connected the Host WebSocket in dev**, so the phantom first connection's close triggered `host_disconnected` and killed every freshly-created Room before the real connection ever opened — this was the actual root cause of Flag #1 (only symptom-patched in the manual-test pass); fixed properly in `useRoomSocket.ts` with a deferred-close-and-cancel-on-remount pattern instead of closing synchronously.
  - **Abandoned-Lobby TTL eviction left connected clients stranded** with no notification — added a `room_closed` broadcast + connection close for this case (`store.py`, `rooms_ws.py`, `main.py`, plus a new `closed` phase/`RoomErrorPanel` render on the frontend).
  - One minor finding left as-is (Tri's call): `RevealPanel` shows "Incorrect" for a Player who simply never answered (timed out), same as an actual wrong answer — no "no answer submitted" distinction.
  - All 77 backend `pytest` cases and 49 frontend `vitest` cases pass; `tsc`/`oxlint` clean; zero errors/exceptions in the backend log across the whole live session.

- **Manual test pass + UI polish** (2026-08-12), handwritten checklists across flashcard display, Review Session, and Game Room. Conducted manual tests, then polished up the design, added a concept navigation bar, and a few more items — card sizing/spacing fixes, a question-type label (flashcard, Review Session, Game Room), a redesigned Review Session Summary (score + navigable question list alongside a detail panel), a Review Session exit button, a Game Room player score/nickname corner badge, and a ~75% size increase across Game Room's UI. Re-verified after: 77/77 backend `pytest`, 49/49 frontend `vitest`, `tsc`/`oxlint` clean.

### Flashcard prototype (`frontend/`, Vite + React + TS)

Built off a hand-written (not agent-generated) fixture Question Set to validate the display/UX independent of the generation pipeline, styled off the Botanical Field Guide reference in `assets/card-themes/`. Split into three builds:

- Build 1 — flashcard display and navigation only (front/back flip, Next/Prev, progress indicator), no editing yet. Grilled and scoped (2026-08-10): `frontend/` dir, Vite + React + TS + npm; CSS Modules porting `botanical-field-guide.html` near-verbatim; TS types mirroring `DESIGN.md`'s data model; fixture as a `.ts` file (`satisfies QuestionSet`, Claude drafted content grounded in `water_cycle_note.txt`, Tri reviewed before it was saved); components `FlashcardView` + `Flashcard` + a plain concept-trim helper function; Vitest unit test for the concept-trim helper only; disabled placeholder Edit button on the card back. Built and verified (2026-08-10): Claude drove Chrome directly against the dev server — flip, Next/Prev with correct boundary disabling, flip-reset-on-nav (via `key={currentIndex}` remount), radio vs checkbox mark shapes, correct-answer highlighting, concept-trim rule (both delimiter and no-delimiter cases), disabled Edit placeholder all confirmed live; no console errors; `npm test` (6/6) and `tsc --noEmit` clean. `water_cycle_note.txt` also removed from `main` (kept on this branch only) at Tri's request, since it only grounds this branch's fixture.
- Build 2 — option to start a Review Session (per `CONTEXT.md`/`requirements.md`'s definition), reachable via a "Start Review" button on the flashcard display. Built and verified (2026-08-10).
- Build 3 — in-place editing from the flashcard view (per DESIGN.md's Editing section: field edits, Save/Cancel, concept rename/reassign). Built and verified (2026-08-10).



## Current State

`requirements.md` considered done — scope fully defined (intended user, needs, use cases, non-goals, acceptance criteria, planned-version ordering). `DESIGN.md` rework considered done: Capacity map, API contract, and Extraction sections added; Pipeline/Concurrent generation/Snippet grounding/Weight reconciliation updated for the per-concept-list restructure and the `gpt-oss-120b` model swap; actual Agent prompts (Analyzer/Generator/Verifier) written, iterated, and merged in from `agents.txt`; further updated 2026-08-13 with the `agent-testing` branch's later architecture (dual-model Generator pool, page-scoped Verifier, verify-loop skip, concurrency cap, prompt-caching reorder, 2 new quality-bar checks) — see Done above. `CONTEXT.md` and `requirements.md` cross-checked against `DESIGN.md` and brought back in sync. All three prototype builds are done: Build 1 (display/nav), Build 2 (Review Session), Build 3 (in-place editing, including concept rename/reassign). Game Room backend and frontend are both built, tested, and merged into `main` (2026-08-11) — see Done above. Agent pipeline (2026-08-13): fully designed and validated on `agent-testing`, now ported into `backend/app/` and live behind FastAPI endpoints (`POST /upload`/`/generate`/etc., all of DESIGN.md's API contract), and Room reconnected to source the session's real generated `QuestionSet` instead of the hardcoded fixture — see Done above. Only remaining gap: the frontend has no upload/Analyzer-checkpoint UI yet, so there's no way to reach the new backend endpoints from the browser — see Todo below.

## Todo

- [x] Game Room implementation, backend + frontend (parts 1-8) — see Done above. Built and pushed to `origin/game-room-build` (2026-08-11).
- [x] Game Room manual-test flags (all 7) fixed and merged into `main` (2026-08-11) — see Done above. Ahead of the original "after all v1.x ships" plan, per Tri's explicit call to bring it in now with the fixture caveat flagged in `CONTEXT.md`.
- [x] Full test pass on the merged/cleaned-up `main`, everything except agent question generation (2026-08-11/12) — see Done above. 77/77 backend, 49/49 frontend, 2 real bugs found and fixed.
- [x] **Backend pipeline port (2026-08-13)** — `agent_testing/*.py` ported into `backend/app/` across 6 layers (models, `call_agent.py`/`generator_pool.py`/`parsers.py`, `pipeline.py` orchestration, `SessionStore`, FastAPI endpoints), each layer confirmed and smoke-tested individually before the next. New code beyond a straight port: `extraction.py` (PDF text extraction — agent-testing never built this, it read a `.txt` file directly), an `on_event`/`on_result` callback threaded through `pipeline.py`/`generator_pool.py` (no globals) so `/generate`'s SSE stream gets real per-concept/per-round progress matching DESIGN.md's `{stage, concept?, round?}` shape, and 2 defense-in-depth validations (`weights_not_confirmed`, `no_active_session`) not in DESIGN.md's literal contract. Known limitation, left as-is: `call_agent.py`'s `CALL_LOG`/`CALL_LOG_HOOK` are still module-level globals (fine for agent-testing's single-run CLI origin; concurrent `/generate` requests will interleave in that internal debug log — doesn't affect API responses). 77/77 existing backend tests passed throughout; full upload→generate→PATCH→session flow smoke-tested offline with live Groq calls stubbed out.
- [x] **Room reconnected to real generated `QuestionSet` (2026-08-13)** — `POST /rooms` now reads the calling session's `SessionStore` state and requires `status == "ready"` (404 `no_question_set` otherwise), replacing the `DIGESTIVE_SYSTEM_QUESTION_SET` default. `fixture.py` itself stays — now purely test data, seeded via a new `tests/conftest.py::room_test_client()` helper (direct `SessionStore` seeding, no live pipeline run needed for Room tests). 2 existing Room tests lost a now-impossible cookie-setting assertion (that behavior is already covered independently by `test_session.py`); 1 new test added for the `no_question_set` path. `CONTEXT.md`'s "TEMPORARY — Game Room's Question Set source" section removed (its whole purpose was flagging this exact gap); `DESIGN.md`'s Room data model/API contract/Room pipeline sections updated to describe the real QuestionSet instead of the fixture. 78/78 backend tests pass.
- [x] **Verifier page-scoping widened to include neighbor pages (2026-08-13)** — `_verify_concept` in `pipeline.py` now expands each Question's referenced page to `{page-1, page, page+1}` before scoping (was: exact referenced pages only), since a fix snippet often needs a caveat/detail stated just before or after the source page. `DESIGN.md`'s Snippet grounding section updated to match, and its existing "~28% token drop" figure flagged as measured under the old (no-neighbor) scoping, not yet re-measured — expect a smaller reduction now that more pages are included per call. 78/78 backend tests pass.
- [x] **New Generator quality-bar bullet on incorrect options (2026-08-13)** — both Generator prompts (initial + patch, `DESIGN.md` and `call_agent.py` kept in sync) gained: "Incorrect options must be closely related to the question, requiring real thought to rule out — but must not convey the same meaning as any correct answer, just reworded." Existing distractor rules covered plausibility/non-duplication; this adds the specific requirement that a distractor stay topically close without just rewording a correct answer. Verifier prompt intentionally left untouched (Tri's call — only the Generators asked for this).
- [ ] Build frontend upload + Analyzer-checkpoint screens — `main`'s frontend currently only has the fixture-driven flashcard view, no upload flow at all. Wire to the new backend endpoints (`/upload`, `/generate` SSE, `PATCH`s, `/session`) now that they exist. Deferred to a later session per Tri's call.

- [x] Agents implemented and pipeline-wired (`agent-testing` branch) — isolated per-call tests done (`agent_testing/agent-test-log.md`), full pipeline (Analyzer → checkpoint → Generator → verify loop → Freeze) wired and run once clean end-to-end (`agent_testing/pipeline-run-log.md`). Two real prompt bugs found and fixed via live testing: Generator-patch was ignoring new fix snippets and reusing the pre-fix `source_quote`; Verifier was flagging `is_select_all` purely from `len(correct_answers)` instead of phrasing/framing. Both fixes copied into `DESIGN.md`'s canonical prompts after confirming with Tri.
- [x] Split-vs-big-call comparison run (2026-08-11), same input (`water_cycle_note.txt`) for all 3 tests: `agent-testing` sequential succeeded clean (27 Questions, 290.5s, 0 rate-limit hits, 163/163 quality checks). `agent-testing` concurrent succeeded but gained nothing (24 Questions, 298.1s — no faster than sequential — uncapped `asyncio.gather` triggered 21 rate-limit hits that serialized it back into a queue anyway). `agent-testing-bigcall` failed both attempts — the single Generator call asking for all ~24 questions at once got truncated before completing (not a 429; likely the model's default completion-token cap). **Decision: drop the big-call approach** — `agent-testing-bigcall` branch deleted (local + remote), its worktree removed. Split design only, going forward. Full comparison + per-call logs kept in a local `agent-comparison-report.md` at the repo root (Tri's copy only, not committed).
- [x] **Token/runtime optimizations on `agent-testing`, implemented 2026-08-11, live-validated 2026-08-12**:
  1. Verify-loop skip: a concept cleared (satisfactory) in an earlier round is never re-verified later, only still-pending concepts get a Verifier call each round. Live-validated 2026-08-12 (163/163 quality checks, exact parity with baseline) — the skip itself hasn't been *exercised* live yet, since every live run so far has resolved in verify round 1.
  2. Concurrency cap: pipeline-wide `asyncio.Semaphore` (`call_agent.CONCURRENCY_CAP=3`). Live-tested 2026-08-12 (`--concurrent`, `water_cycle_note.txt`): reduces rate-limit hits (21 uncapped → 13 capped) but does **not** recover wall-clock time (373.6s capped vs. 365.4s sequential) — single model's 8K TPM ceiling still the bottleneck. **Decision: sequential stays the default**; concurrency's real payoff needed the multi-model split below.
  3. Explicit `max_tokens`: per-call dynamic sizing, wired through all 4 pipeline call sites. Live-tested 2026-08-12 — no truncation failures.
  4. Concept batching (`--batch-size N`): tried, **failed live-tested 2026-08-12 at `batch_size=3`** — a Verifier-batch call hard-crashed with HTTP 413 (Groq's 8,000 TPM per-request cap, requested 8,692). **Decision: parked at `batch_size=1`, then removed entirely** — feature deleted from `pipeline.py`/`call_agent.py`/`parsers.py`.
  5. Verifier page-scoping: `_extract_pages()` sends only the Note pages a concept's Questions actually reference. Live-tested 2026-08-12 — Verifier input tokens dropped ~28% per call, no quality loss. **Ported into `DESIGN.md`'s canonical Snippet grounding section 2026-08-13 (see below).**
  - 2026-08-12 live-validation result (sequential, all optimizations active): 27 Questions, 163/163 quality checks — exact parity with the no-optimization baseline. No quality regression. Total tokens down 3.2%, total time up 25.8% (attributable to one bad-luck retry + model-latency variance, not the optimizations).
- [x] **Dual-model Generator pool implemented and live-tested (`agent-testing` branch, 2026-08-12)**: Generator's initial-pass and patch-pass calls dispatch through a two-worker pool (`generator_pool.py`) across `llama-3.3-70b-versatile` and `openai/gpt-oss-20b`, with per-job cross-model failover. Analyzer and Verifier stay on `gpt-oss-120b`. Phase 1 (isolated Generator): dynamic dispatch confirmed across both models, no contract violations. Phase 2 (full pipeline, `water_cycle_note.txt`, sequential): 24 Questions, 145/145 quality checks (100%), 244.0s — faster than both single-model baselines, a real concurrency win. **Ported into `DESIGN.md`'s canonical Concurrent generation section 2026-08-13 (see below).**
- [x] **New-input quality pass (`french_revolution_note.txt`, history topic, done 2026-08-13)**: full pipeline run against a new domain, every call reviewed by Tri. Problems found and fixed: (1) Verifier missed an ambiguous-distractor case (a "wrong" option the Note itself also asserted true) — tightened both Generator's and Verifier's quality-bar prompts. (2) Verifier had no cross-question duplicate check — added a duplicate-check criterion. (3)/(4) Select-All near-absence and a stray blank Generator line investigated, confirmed non-issues, no fix needed. (5) A cosmetic same-position answer-key pattern — no fix, not worth the code. **Both new quality-bar checks ported into `DESIGN.md`'s canonical Agent prompts and Verify loop detail 2026-08-13 (see below).**
  - Optimization: Groq prompt caching (automatic, 50% discount on cached input tokens, `gpt-oss-120b`/`gpt-oss-20b` only) — Generator-initial/patch/Verifier prompt templates reordered (static instructions first, variable content last) to maximize the cacheable prefix. **Ported into `DESIGN.md`'s canonical Agent prompts 2026-08-13 (see below).**
  - Incidental bugs found and fixed live: Windows console `UnicodeEncodeError` on non-ASCII model output (worked around via `PYTHONIOENCODING=utf-8`, not yet a permanent code fix); Verifier `max_tokens` budget too tight for the lengthened quality-bar criteria (bumped 1000/400 → 1500/600); a false-positive fabricated-snippet rejection on a Unicode non-breaking hyphen (normalization now folds hyphen-class Unicode variants to ASCII `-`).
  - **Still open, carried into this merge**: a concept can be allocated `question_count=0` by weight-reconciliation, causing the Generator to be asked for "exactly 0 Questions" — usually recovers after 1-2 wasted retries, not guaranteed. Not yet fixed. A full clean end-to-end re-validation run (all fixes + prompt caching + `cached_tokens` logging) was never completed — interrupted twice by Groq rate limits, once by the daily token cap.
- [x] **`DESIGN.md` merge into `main` (2026-08-13)**: ported agent-testing's generation-pipeline advances into main's canonical `DESIGN.md`, one section at a time with Tri's confirmation each time — Capacity map (Generator-pool rate limits, concurrency cap, completion-token cap rows), Agents section (dual-model pool, page-scoped Verifier), Concurrent generation (verify-loop skip, concurrency cap results, Dual-model Generator pool, explicit `max_tokens`), Snippet grounding (page-scoping trade-off), all 3 Agent prompts (reordered for caching, new ambiguous-distractor and duplicate-question quality-bar checks), Verify loop detail (same 2 new criteria, pending-skip loop-count note). One stale leftover ("FULL Note" in the top pipeline diagram) found and fixed to match. Backend code port (bringing `agent_testing/*.py` into `backend/app/`, wiring FastAPI endpoints) and Game Room's fixture→real-QuestionSet reconnect are the remaining merge steps — see Todo below.



## Deferred (not started, tracked in `requirements.md` Planned Versions unless noted)

- Theme switching — wiring the four saved reference themes (`assets/card-themes/`) in as selectable alternatives to the default.
- v1.x: multi-PDF merge, regeneration controls, difficulty levels, larger file support, spaced-repetition/requeue mechanics.
- Reconnecting Game Room's `QuestionSet` source from the hardcoded fixture to the real v1 pipeline's generated `QuestionSet`, once v1 generation is finished — see `CONTEXT.md`'s note on this (not tracked in `requirements.md`, which now describes the fixture-based wiring as final).

